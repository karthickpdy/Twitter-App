<table class="profile">
    <tr>
        <td class="main">
            <h2>
                <%=image_tag(grav gravatar_for @user, :alt=>"Sample App", :class => "round") %>
                    <%=h @user.name %>
            </h2>
        </td>
        <td class="sidebar round">
            <strong>Name</strong>
            <%=h @user.name %>
                <br />
                <strong>URL</strong>
                <%=l ink_to user_path(@user), @user %>
        </td>
    </tr>
</table>